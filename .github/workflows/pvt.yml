name: Example Workflow

on:
  repository_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          repository: harshulgupt/json_pvt
          ref: main
      - name: Check changes
        run: echo "Checking changes made to repository files in json_pvt repository"
